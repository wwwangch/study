server:
  port: 8081
  servlet:
    context-path: /curator
spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://127.0.0.1/wch?useSSL=false&serverTimezone=Asia/Shanghai
      username: root
      password: root

logging:
  config: classpath:log4j2-spring.xml
